# 智慧餐厅 数据库字典

> 数据库：`smart_restaurant` ｜ MySQL 8.0 ｜ InnoDB ｜ `utf8mb4_general_ci` ｜ 43 张表 + 6 个报表视图

> 关联完整性：逻辑外键 + 索引保障（不建物理外键约束），ER 关系见【第二部分】

## 目录

1. [第一部分 数据表基础说明](#1)
2. [第二部分 表间关联关系（ER）](#2)
3. [第三部分 字段详细说明](#3)
4. [附录 报表视图](#4)

## 设计约定

- 主键统一为 `id BIGINT UNSIGNED AUTO_INCREMENT`；软删除统一使用 `is_deleted`（0否/1是）；时间统一为 `create_time`/`update_time`。
- 金额统一 `DECIMAL(10,2)`；状态字段统一 `TINYINT` 并在注释中标注枚举含义；全部表使用 `utf8mb4` + `utf8mb4_general_ci`。
- 表间关联采用**逻辑外键**（同名列 + 索引），由应用层保证一致性；本设计不创建物理外键约束，便于水平扩展与批量数据导入。
- 并发控制：`orders.version` 乐观锁；`payment_record.idempotency_key` 支付幂等键。

## 第一部分 数据表基础说明

| 序号 | 表名 | 中文释义 | 业务用途 |
|---|---|---|---|
| 1 | `sys_role` | 角色表 | 定义系统角色（管理员/店长/服务员/收银/后厨），支撑权限分配 |
| 2 | `sys_permission` | 权限表 | 定义商户端菜单与操作权限点，与角色多对多绑定 |
| 3 | `sys_role_permission` | 角色-权限关联表 | 角色与权限的多对多中间表，实现 RBAC 权限分配 |
| 4 | `sys_user` | 员工/商户账号表 | 员工/商户后台登录账号，含角色与所属门店 |
| 5 | `staff_schedule` | 员工排班表 | 员工排班计划（早晚班/时间），支撑门店人力管理 |
| 6 | `staff_login_log` | 员工登录日志表 | 员工登录日志，用于账号安全审计与异常登录排查 |
| 7 | `store` | 门店表 | 门店主数据，含地址/坐标/营业时间/配送参数 |
| 8 | `store_payment_setting` | 门店支付设置表 | 门店支付渠道配置（收款码/费率） |
| 9 | `member_category` | 会员类别表 | 会员等级类别（银卡/金卡/黑金卡），定义充值门槛与折扣 |
| 10 | `member` | 客户/会员表 | 会员主档案（微信openid/手机号/余额/积分/等级） |
| 11 | `member_address` | 客户收货地址表 | 会员收货地址，支撑外卖配送 |
| 12 | `member_bank_card` | 会员银行卡表 | 会员绑定银行卡，支撑提现/退款原路返回 |
| 13 | `member_balance_record` | 会员钱包交易记录表 | 会员钱包余额流水，记账可追溯、可对账 |
| 14 | `member_points_record` | 会员积分记录表 | 会员积分流水（获得/消费），支撑积分体系 |
| 15 | `member_recharge_record` | 会员充值记录表 | 会员充值记录（含赠送金额与支付流水） |
| 16 | `red_packet` | 会员红包表 | 会员红包（充值奖励/营销发放） |
| 17 | `dish_category` | 菜品分类表 | 菜品分类（招牌热菜/家常小炒/饮品等） |
| 18 | `dish_taste` | 菜品口味表 | 菜品口味（辣度/做法等可选项） |
| 19 | `dish_spec` | 菜品规格表 | 菜品规格（大/中/小份等） |
| 20 | `ingredient_category` | 原料类别表 | 原料类别（生鲜/粮油/调料等） |
| 21 | `ingredient` | 原料表 | 原料档案（名称/规格/单位/参考成本） |
| 22 | `dish` | 菜品表 | 菜品主数据（价格/分类/口味/规格/上下架/销量） |
| 23 | `dish_ingredient_rel` | 菜品-原料关联表 | 菜品与原料多对多关联，支撑成本核算与反查 |
| 24 | `dish_review` | 菜品评价表 | 菜品评价（评分/图文/匿名/商家回复） |
| 25 | `table_type` | 桌型表 | 桌型定义（2人/4人/6人/包间） |
| 26 | `dining_table` | 台桌表 | 台桌档案（桌号/桌型/二维码/状态） |
| 27 | `queue` | 排队取号记录表 | 排队取号记录（叫号/过号/就位），支撑取号叫号流程 |
| 28 | `reservation` | 餐桌预约表 | 餐桌预约（时段/人数/预留桌） |
| 29 | `cart` | 购物车表 | 购物车（会员会话级点餐暂存） |
| 30 | `orders` | 订单主表（coupon_id为逻辑外键，关联coupon.id） | 订单主表（堂食/外卖，金额拆分，状态机，乐观锁） |
| 31 | `order_detail` | 订单明细表 | 订单明细（菜品快照/数量/小计/制作状态/已退金额） |
| 32 | `order_status_log` | 订单状态日志表 | 订单状态流转日志，业务审计留痕 |
| 33 | `payment_record` | 支付记录表 | 支付记录（渠道/幂等键/交易号/回调） |
| 34 | `refund` | 退单记录表 | 退单/退款记录（退菜/整单，金额与原因） |
| 35 | `coupon` | 优惠券表 | 优惠券定义（满减/折扣/立减，使用范围） |
| 36 | `member_coupon` | 用户优惠券表 | 会员领用优惠券（状态/有效期/使用订单） |
| 37 | `dish_stock` | 菜品库存表 | 菜品库存（按门店按菜品） |
| 38 | `ingredient_stock` | 原料库存表 | 原料库存（按门店按原料） |
| 39 | `stock_check_dish` | 菜品盘点单表 | 菜品盘点单（盘点批次/差异） |
| 40 | `stock_check_ingredient` | 原料盘点单表 | 原料盘点单（盘点批次/差异） |
| 41 | `printer` | 打印机表 | 门店打印机绑定（打印任务/启用状态） |
| 42 | `receipt_template` | 小票模板表 | 小票模板（结账单/外卖单/排队单模板内容） |
| 43 | `feedback` | 意见反馈表 | 客户意见反馈 |

## 第二部分 表间关联关系（ER）

关联类型约定：`1:N`＝父表 1 条对应子表 N 条；`N:M`＝多对多（由中间表承载）。级联策略：`CASCADE`＝删除父表级联删除子表；`RESTRICT`＝有子记录时禁止删除父记录；`LOGICAL`＝仅逻辑关联（无级联约束）。

| 子表 | 父表 | 关联字段(子.字段 = 父.字段) | 关联类型 | 级联策略 |
|---|---|---|---|---|
| `sys_role_permission` | `sys_role` | `sys_role_permission.role_id` = `sys_role.id` | N:M(中间表) | CASCADE |
| `sys_role_permission` | `sys_permission` | `sys_role_permission.permission_id` = `sys_permission.id` | N:M(中间表) | CASCADE |
| `sys_user` | `sys_role` | `sys_user.role_id` = `sys_role.id` | N:1 | RESTRICT |
| `staff_schedule` | `sys_user` | `staff_schedule.staff_id` = `sys_user.id` | N:1 | CASCADE |
| `staff_login_log` | `sys_user` | `staff_login_log.staff_id` = `sys_user.id` | N:1 | CASCADE |
| `store_payment_setting` | `store` | `store_payment_setting.store_id` = `store.id` | N:1 | CASCADE |
| `member` | `member_category` | `member.member_category_id` = `member_category.id` | N:1 | RESTRICT |
| `member` | `store` | `member.register_store_id` = `store.id` | N:1 | RESTRICT |
| `member_address` | `member` | `member_address.member_id` = `member.id` | N:1 | CASCADE |
| `member_bank_card` | `member` | `member_bank_card.member_id` = `member.id` | N:1 | CASCADE |
| `member_balance_record` | `member` | `member_balance_record.member_id` = `member.id` | N:1 | CASCADE |
| `member_points_record` | `member` | `member_points_record.member_id` = `member.id` | N:1 | CASCADE |
| `member_recharge_record` | `member` | `member_recharge_record.member_id` = `member.id` | N:1 | RESTRICT |
| `member_recharge_record` | `store` | `member_recharge_record.store_id` = `store.id` | N:1 | RESTRICT |
| `red_packet` | `member` | `red_packet.member_id` = `member.id` | N:1 | CASCADE |
| `dish_category` | `store` | `dish_category.store_id` = `store.id` | N:1 | CASCADE |
| `dish_taste` | `store` | `dish_taste.store_id` = `store.id` | N:1 | CASCADE |
| `dish_spec` | `store` | `dish_spec.store_id` = `store.id` | N:1 | CASCADE |
| `ingredient_category` | `store` | `ingredient_category.store_id` = `store.id` | N:1 | CASCADE |
| `ingredient` | `store` | `ingredient.store_id` = `store.id` | N:1 | CASCADE |
| `ingredient` | `ingredient_category` | `ingredient.category_id` = `ingredient_category.id` | N:1 | RESTRICT |
| `dish` | `store` | `dish.store_id` = `store.id` | N:1 | CASCADE |
| `dish` | `dish_category` | `dish.category_id` = `dish_category.id` | N:1 | RESTRICT |
| `dish` | `dish_taste` | `dish.taste_id` = `dish_taste.id` | N:1 | RESTRICT |
| `dish` | `dish_spec` | `dish.spec_id` = `dish_spec.id` | N:1 | RESTRICT |
| `dish_ingredient_rel` | `dish` | `dish_ingredient_rel.dish_id` = `dish.id` | N:M(中间表) | CASCADE |
| `dish_ingredient_rel` | `ingredient` | `dish_ingredient_rel.ingredient_id` = `ingredient.id` | N:M(中间表) | CASCADE |
| `dish_review` | `dish` | `dish_review.dish_id` = `dish.id` | N:1 | CASCADE |
| `dish_review` | `member` | `dish_review.member_id` = `member.id` | N:1 | RESTRICT |
| `table_type` | `store` | `table_type.store_id` = `store.id` | N:1 | CASCADE |
| `dining_table` | `store` | `dining_table.store_id` = `store.id` | N:1 | CASCADE |
| `dining_table` | `table_type` | `dining_table.table_type_id` = `table_type.id` | N:1 | RESTRICT |
| `queue` | `store` | `queue.store_id` = `store.id` | N:1 | CASCADE |
| `queue` | `table_type` | `queue.table_type_id` = `table_type.id` | N:1 | RESTRICT |
| `reservation` | `store` | `reservation.store_id` = `store.id` | N:1 | CASCADE |
| `reservation` | `member` | `reservation.member_id` = `member.id` | N:1 | RESTRICT |
| `reservation` | `dining_table` | `reservation.table_id` = `dining_table.id` | N:1 | RESTRICT |
| `cart` | `member` | `cart.member_id` = `member.id` | N:1 | CASCADE |
| `cart` | `dish` | `cart.dish_id` = `dish.id` | N:1 | RESTRICT |
| `orders` | `store` | `orders.store_id` = `store.id` | N:1 | RESTRICT |
| `orders` | `member` | `orders.member_id` = `member.id` | N:1 | RESTRICT |
| `orders` | `dining_table` | `orders.table_id` = `dining_table.id` | N:1 | RESTRICT |
| `orders` | `coupon` | `orders.coupon_id` = `coupon.id` | N:1 | LOGICAL |
| `order_detail` | `orders` | `order_detail.order_id` = `orders.id` | N:1 | CASCADE |
| `order_detail` | `dish` | `order_detail.dish_id` = `dish.id` | N:1 | RESTRICT |
| `order_status_log` | `orders` | `order_status_log.order_id` = `orders.id` | N:1 | CASCADE |
| `payment_record` | `orders` | `payment_record.order_id` = `orders.id` | N:1 | RESTRICT |
| `payment_record` | `store` | `payment_record.store_id` = `store.id` | N:1 | RESTRICT |
| `payment_record` | `member` | `payment_record.member_id` = `member.id` | N:1 | RESTRICT |
| `refund` | `orders` | `refund.order_id` = `orders.id` | N:1 | RESTRICT |
| `coupon` | `store` | `coupon.store_id` = `store.id` | N:1 | RESTRICT |
| `member_coupon` | `member` | `member_coupon.member_id` = `member.id` | N:1 | CASCADE |
| `member_coupon` | `coupon` | `member_coupon.coupon_id` = `coupon.id` | N:1 | CASCADE |
| `dish_stock` | `store` | `dish_stock.store_id` = `store.id` | N:1 | CASCADE |
| `dish_stock` | `dish` | `dish_stock.dish_id` = `dish.id` | N:1 | CASCADE |
| `ingredient_stock` | `store` | `ingredient_stock.store_id` = `store.id` | N:1 | CASCADE |
| `ingredient_stock` | `ingredient` | `ingredient_stock.ingredient_id` = `ingredient.id` | N:1 | CASCADE |
| `stock_check_dish` | `store` | `stock_check_dish.store_id` = `store.id` | N:1 | RESTRICT |
| `stock_check_dish` | `dish` | `stock_check_dish.dish_id` = `dish.id` | N:1 | RESTRICT |
| `stock_check_ingredient` | `store` | `stock_check_ingredient.store_id` = `store.id` | N:1 | RESTRICT |
| `stock_check_ingredient` | `ingredient` | `stock_check_ingredient.ingredient_id` = `ingredient.id` | N:1 | RESTRICT |
| `printer` | `store` | `printer.store_id` = `store.id` | N:1 | CASCADE |

### 父表 → 子表（一对多）速查

- **sys_role** → sys_role_permission, sys_user
- **sys_permission** → sys_role_permission
- **sys_user** → staff_schedule, staff_login_log
- **store** → store_payment_setting, member, member_recharge_record, dish_category, dish_taste, dish_spec, ingredient_category, ingredient, dish, table_type, dining_table, queue, reservation, orders, payment_record, coupon, dish_stock, ingredient_stock, stock_check_dish, stock_check_ingredient, printer
- **member_category** → member
- **member** → member_address, member_bank_card, member_balance_record, member_points_record, member_recharge_record, red_packet, dish_review, reservation, cart, orders, payment_record, member_coupon
- **ingredient_category** → ingredient
- **dish_category** → dish
- **dish_taste** → dish
- **dish_spec** → dish
- **dish** → dish_ingredient_rel, dish_review, cart, order_detail, dish_stock, stock_check_dish
- **ingredient** → dish_ingredient_rel, ingredient_stock, stock_check_ingredient
- **table_type** → dining_table, queue
- **dining_table** → reservation, orders
- **coupon** → orders, member_coupon
- **orders** → order_detail, order_status_log, payment_record, refund

## 第三部分 字段详细说明

## 一、系统权限与账号

### sys_role — 角色表

业务用途：定义系统角色（管理员/店长/服务员/收银/后厨），支撑权限分配

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 角色ID |
| `role_name` | VARCHAR(50) | 否 | — |  | 角色名称 |
| `role_code` | VARCHAR(50) | 否 | — |  | 角色编码 |
| `description` | VARCHAR(255) | 是 | NULL |  | 角色描述 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_role_code` (`role_code`)（唯一）

### sys_permission — 权限表

业务用途：定义商户端菜单与操作权限点，与角色多对多绑定

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 权限ID |
| `permission_name` | VARCHAR(50) | 否 | — |  | 权限名称 |
| `permission_code` | VARCHAR(50) | 否 | — |  | 权限编码（接口/按钮标识） |
| `menu_url` | VARCHAR(200) | 是 | NULL |  | 菜单URL |
| `parent_id` | BIGINT UNSIGNED | 否 | 0 |  | 父级ID，0为顶级 |
| `sort` | INT | 否 | 0 |  | 排序号 |
| `status` | TINYINT | 否 | 1 |  | 状态 1启用 0停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_permission_code` (`permission_code`)（唯一）
- `INDEX` `idx_parent_id` (`parent_id`)

### sys_role_permission — 角色-权限关联表

业务用途：角色与权限的多对多中间表，实现 RBAC 权限分配

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 主键ID |
| `role_id` | BIGINT UNSIGNED | 否 | — |  | 角色ID |
| `permission_id` | BIGINT UNSIGNED | 否 | — |  | 权限ID |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_role_permission` (`role_id`, `permission_id`)（唯一）
- `INDEX` `idx_permission_id` (`permission_id`)

**逻辑外键：**
- `sys_role_permission.role_id` → `sys_role.id`（CASCADE）
- `sys_role_permission.permission_id` → `sys_permission.id`（CASCADE）

### sys_user — 员工/商户账号表

业务用途：员工/商户后台登录账号，含角色与所属门店

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 员工ID |
| `store_id` | BIGINT UNSIGNED | 是 | NULL |  | 所属门店ID（总店长为空） |
| `username` | VARCHAR(50) | 否 | — |  | 登录账号（手机号） |
| `password` | VARCHAR(255) | 否 | — |  | 登录密码（生产用bcrypt加密存储） |
| `real_name` | VARCHAR(50) | 是 | NULL |  | 姓名 |
| `staff_no` | VARCHAR(20) | 是 | NULL |  | 员工工号 |
| `email` | VARCHAR(100) | 是 | NULL |  | 邮箱 |
| `phone` | VARCHAR(20) | 是 | NULL |  | 手机号 |
| `id_card` | VARCHAR(20) | 是 | NULL |  | 身份证号 |
| `role_id` | BIGINT UNSIGNED | 否 | — |  | 角色ID |
| `avatar` | VARCHAR(255) | 是 | NULL |  | 头像URL |
| `status` | TINYINT | 否 | 1 |  | 账号状态 1在职 0离职 |
| `last_login_time` | DATETIME | 是 | NULL |  | 最后登录时间 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_username` (`username`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)
- `INDEX` `idx_role_id` (`role_id`)

**逻辑外键：**
- `sys_user.role_id` → `sys_role.id`（RESTRICT）

### staff_schedule — 员工排班表

业务用途：员工排班计划（早晚班/时间），支撑门店人力管理

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 排班ID |
| `staff_id` | BIGINT UNSIGNED | 否 | — |  | 员工ID |
| `work_date` | DATE | 否 | — |  | 排班日期 |
| `shift_type` | TINYINT | 否 | 1 |  | 班次 1早班 2中班 3晚班 |
| `start_time` | TIME | 是 | NULL |  | 上班时间 |
| `end_time` | TIME | 是 | NULL |  | 下班时间 |
| `remark` | VARCHAR(255) | 是 | NULL |  | 备注 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_staff_date` (`staff_id`, `work_date`)（唯一）
- `INDEX` `idx_work_date` (`work_date`)

**逻辑外键：**
- `staff_schedule.staff_id` → `sys_user.id`（CASCADE）

### staff_login_log — 员工登录日志表

业务用途：员工登录日志，用于账号安全审计与异常登录排查

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 日志ID |
| `staff_id` | BIGINT UNSIGNED | 否 | — |  | 员工ID |
| `login_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 登录时间 |
| `login_ip` | VARCHAR(50) | 是 | NULL |  | 登录IP |
| `device` | VARCHAR(255) | 是 | NULL |  | 登录设备 |
| `login_result` | TINYINT | 否 | 1 |  | 结果 1成功 0失败 |
| `fail_reason` | VARCHAR(255) | 是 | NULL |  | 失败原因 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_staff_id` (`staff_id`)
- `INDEX` `idx_login_time` (`login_time`)

**逻辑外键：**
- `staff_login_log.staff_id` → `sys_user.id`（CASCADE）


## 二、门店与门店设置

### store — 门店表

业务用途：门店主数据，含地址/坐标/营业时间/配送参数

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 门店ID |
| `store_no` | VARCHAR(20) | 否 | — |  | 门店编号 |
| `store_name` | VARCHAR(100) | 否 | — |  | 门店名称 |
| `province` | VARCHAR(50) | 是 | NULL |  | 省份 |
| `city` | VARCHAR(50) | 是 | NULL |  | 城市 |
| `district` | VARCHAR(50) | 是 | NULL |  | 区县 |
| `address` | VARCHAR(255) | 是 | NULL |  | 详细地址 |
| `longitude` | DECIMAL(10,6) | 是 | NULL |  | 经度 |
| `latitude` | DECIMAL(10,6) | 是 | NULL |  | 纬度 |
| `phone` | VARCHAR(20) | 是 | NULL |  | 门店电话 |
| `contact_name` | VARCHAR(50) | 是 | NULL |  | 负责人 |
| `business_status` | TINYINT | 否 | 1 |  | 营业状态 1营业 0打烊 |
| `open_time` | TIME | 是 | NULL |  | 开始营业时间 |
| `close_time` | TIME | 是 | NULL |  | 结束营业时间 |
| `dine_in_enabled` | TINYINT | 否 | 1 |  | 是否允许堂食 1是 0否 |
| `takeout_enabled` | TINYINT | 否 | 1 |  | 是否允许外卖 1是 0否 |
| `delivery_radius` | DECIMAL(5,2) | 否 | 5.00 |  | 配送范围(km) |
| `min_order_amount` | DECIMAL(10,2) | 否 | 20.00 |  | 起送价(元) |
| `delivery_fee` | DECIMAL(10,2) | 否 | 0.00 |  | 基础配送费(元) |
| `delivery_free_threshold` | DECIMAL(10,2) | 否 | 0.00 |  | 免配送费门槛(元) |
| `delivery_start_time` | TIME | 是 | NULL |  | 配送开始时间 |
| `delivery_end_time` | TIME | 是 | NULL |  | 配送结束时间 |
| `logo` | VARCHAR(255) | 是 | NULL |  | 门店Logo |
| `description` | VARCHAR(500) | 是 | NULL |  | 门店简介 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_store_no` (`store_no`)（唯一）
- `INDEX` `idx_city` (`city`)
- `INDEX` `idx_business_status` (`business_status`)

### store_payment_setting — 门店支付设置表

业务用途：门店支付渠道配置（收款码/费率）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 主键ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `pay_type` | VARCHAR(20) | 否 | — |  | 支付类型 WECHAT微信 ALIPAY支付宝 MEMBER会员余额 CASH现金 |
| `is_enabled` | TINYINT | 否 | 1 |  | 是否启用 1启用 0停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_store_paytype` (`store_id`, `pay_type`)（唯一）

**逻辑外键：**
- `store_payment_setting.store_id` → `store.id`（CASCADE）


## 三、客户 / 会员

### member_category — 会员类别表

业务用途：会员等级类别（银卡/金卡/黑金卡），定义充值门槛与折扣

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 会员类别ID |
| `category_name` | VARCHAR(50) | 否 | — |  | 类别名称 |
| `discount_rate` | DECIMAL(3,2) | 否 | 1.00 |  | 折扣率(0.90=9折) |
| `recharge_rule` | VARCHAR(255) | 是 | NULL |  | 充值规则说明 |
| `description` | VARCHAR(255) | 是 | NULL |  | 类别描述 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）

### member — 客户/会员表

业务用途：会员主档案（微信openid/手机号/余额/积分/等级）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 客户ID |
| `openid` | VARCHAR(64) | 是 | NULL |  | 微信openid |
| `unionid` | VARCHAR(64) | 是 | NULL |  | 微信unionid |
| `nickname` | VARCHAR(100) | 是 | NULL |  | 昵称 |
| `avatar` | VARCHAR(255) | 是 | NULL |  | 头像URL |
| `gender` | TINYINT | 否 | 0 |  | 性别 0未知 1男 2女 |
| `phone` | VARCHAR(20) | 是 | NULL |  | 绑定手机号 |
| `real_name` | VARCHAR(50) | 是 | NULL |  | 真实姓名 |
| `id_card` | VARCHAR(20) | 是 | NULL |  | 身份证号（钱包实名） |
| `member_no` | VARCHAR(32) | 是 | NULL |  | 会员卡号 |
| `member_category_id` | BIGINT UNSIGNED | 是 | NULL |  | 会员类别ID |
| `balance` | DECIMAL(10,2) | 否 | 0.00 |  | 钱包余额(元) |
| `total_points` | INT | 否 | 0 |  | 累计积分 |
| `available_points` | INT | 否 | 0 |  | 可用积分 |
| `pay_password` | VARCHAR(255) | 是 | NULL |  | 支付密码（bcrypt加密） |
| `register_store_id` | BIGINT UNSIGNED | 是 | NULL |  | 注册门店ID |
| `status` | TINYINT | 否 | 1 |  | 状态 1正常 0冻结 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_openid` (`openid`)（唯一）
- `UNIQUE` `uk_member_no` (`member_no`)（唯一）
- `UNIQUE` `uk_phone` (`phone`)（唯一）
- `INDEX` `idx_member_category` (`member_category_id`)
- `INDEX` `idx_register_store` (`register_store_id`)

**逻辑外键：**
- `member.member_category_id` → `member_category.id`（RESTRICT）
- `member.register_store_id` → `store.id`（RESTRICT）

### member_address — 客户收货地址表

业务用途：会员收货地址，支撑外卖配送

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 地址ID |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `contact_name` | VARCHAR(50) | 否 | — |  | 联系人姓名 |
| `contact_phone` | VARCHAR(20) | 否 | — |  | 联系人电话 |
| `province` | VARCHAR(50) | 是 | NULL |  | 省份 |
| `city` | VARCHAR(50) | 是 | NULL |  | 城市 |
| `district` | VARCHAR(50) | 是 | NULL |  | 区县 |
| `detail_address` | VARCHAR(255) | 否 | — |  | 详细地址 |
| `longitude` | DECIMAL(10,6) | 是 | NULL |  | 经度 |
| `latitude` | DECIMAL(10,6) | 是 | NULL |  | 纬度 |
| `tag` | VARCHAR(20) | 是 | NULL |  | 地址标签 家/公司 |
| `is_default` | TINYINT | 否 | 0 |  | 是否默认 1是 0否 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_member_id` (`member_id`)

**逻辑外键：**
- `member_address.member_id` → `member.id`（CASCADE）

### member_bank_card — 会员银行卡表

业务用途：会员绑定银行卡，支撑提现/退款原路返回

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 银行卡ID |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `bank_name` | VARCHAR(50) | 是 | NULL |  | 开户行 |
| `card_no` | VARCHAR(32) | 否 | — |  | 银行卡号 |
| `holder_name` | VARCHAR(50) | 是 | NULL |  | 持卡人姓名 |
| `is_default` | TINYINT | 否 | 0 |  | 是否默认 1是 0否 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_member_id` (`member_id`)

**逻辑外键：**
- `member_bank_card.member_id` → `member.id`（CASCADE）

### member_balance_record — 会员钱包交易记录表

业务用途：会员钱包余额流水，记账可追溯、可对账

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 记录ID |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `change_amount` | DECIMAL(10,2) | 否 | — |  | 变动金额（正=收入 负=支出） |
| `balance_after` | DECIMAL(10,2) | 否 | — |  | 变动后余额 |
| `change_type` | VARCHAR(30) | 否 | — |  | 变动类型 RECHARGE充值 CONSUME消费 REFUND退款 RED_PACKET红包 SCAN_PAY扫码付 |
| `source_type` | VARCHAR(30) | 是 | NULL |  | 来源类型 ORDER订单 RECHARGE充值单等 |
| `source_id` | BIGINT UNSIGNED | 是 | NULL |  | 来源ID |
| `remark` | VARCHAR(255) | 是 | NULL |  | 备注 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_member_id` (`member_id`)
- `INDEX` `idx_change_type` (`change_type`)
- `INDEX` `idx_source` (`source_type`, `source_id`)

**逻辑外键：**
- `member_balance_record.member_id` → `member.id`（CASCADE）

### member_points_record — 会员积分记录表

业务用途：会员积分流水（获得/消费），支撑积分体系

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 记录ID |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `points_change` | INT | 否 | — |  | 积分变动（正=增加 负=扣减） |
| `points_after` | INT | 否 | — |  | 变动后积分 |
| `change_type` | VARCHAR(30) | 否 | — |  | 变动类型 CONSUME消费获得 EXCHANGE积分兑换 REFUND退款扣回 ACTIVITY活动赠送 |
| `source_type` | VARCHAR(30) | 是 | NULL |  | 来源类型 |
| `source_id` | BIGINT UNSIGNED | 是 | NULL |  | 来源ID |
| `remark` | VARCHAR(255) | 是 | NULL |  | 备注 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_member_id` (`member_id`)
- `INDEX` `idx_change_type` (`change_type`)

**逻辑外键：**
- `member_points_record.member_id` → `member.id`（CASCADE）

### member_recharge_record — 会员充值记录表

业务用途：会员充值记录（含赠送金额与支付流水）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 充值记录ID |
| `recharge_no` | VARCHAR(32) | 否 | — |  | 充值单号 |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `store_id` | BIGINT UNSIGNED | 是 | NULL |  | 充值门店ID |
| `recharge_amount` | DECIMAL(10,2) | 否 | — |  | 充值金额(元) |
| `gift_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 赠送金额(元) |
| `pay_type` | VARCHAR(20) | 是 | NULL |  | 支付方式 WECHAT/ALIPAY/CASH |
| `status` | TINYINT | 否 | 1 |  | 状态 1成功 0失败 |
| `operator_id` | BIGINT UNSIGNED | 是 | NULL |  | 操作收银员工ID |
| `recharge_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 充值时间 |
| `remark` | VARCHAR(255) | 是 | NULL |  | 备注 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_recharge_no` (`recharge_no`)（唯一）
- `INDEX` `idx_member_id` (`member_id`)
- `INDEX` `idx_store_id` (`store_id`)

**逻辑外键：**
- `member_recharge_record.member_id` → `member.id`（RESTRICT）
- `member_recharge_record.store_id` → `store.id`（RESTRICT）

### red_packet — 会员红包表

业务用途：会员红包（充值奖励/营销发放）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 红包ID |
| `red_packet_no` | VARCHAR(32) | 否 | — |  | 红包编号 |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `store_id` | BIGINT UNSIGNED | 是 | NULL |  | 所属门店ID |
| `amount` | DECIMAL(10,2) | 否 | — |  | 红包金额(元) |
| `source` | VARCHAR(30) | 是 | NULL |  | 来源 RECHARGE_REWARD充值返利 ACTIVITY活动 |
| `status` | TINYINT | 否 | 1 |  | 状态 1未使用 2已使用 3已过期 |
| `expire_time` | DATETIME | 是 | NULL |  | 过期时间 |
| `use_time` | DATETIME | 是 | NULL |  | 使用时间 |
| `order_id` | BIGINT UNSIGNED | 是 | NULL |  | 使用订单ID |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_red_packet_no` (`red_packet_no`)（唯一）
- `INDEX` `idx_member_id` (`member_id`)

**逻辑外键：**
- `red_packet.member_id` → `member.id`（CASCADE）


## 四、菜品与商品

### dish_category — 菜品分类表

业务用途：菜品分类（招牌热菜/家常小炒/饮品等）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 分类ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `category_name` | VARCHAR(50) | 否 | — |  | 分类名称 |
| `sort` | INT | 否 | 0 |  | 排序号 |
| `status` | TINYINT | 否 | 1 |  | 状态 1启用 0停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)

**逻辑外键：**
- `dish_category.store_id` → `store.id`（CASCADE）

### dish_taste — 菜品口味表

业务用途：菜品口味（辣度/做法等可选项）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 口味ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `taste_name` | VARCHAR(50) | 否 | — |  | 口味名称 |
| `sort` | INT | 否 | 0 |  | 排序号 |
| `status` | TINYINT | 否 | 1 |  | 状态 1启用 0停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)

**逻辑外键：**
- `dish_taste.store_id` → `store.id`（CASCADE）

### dish_spec — 菜品规格表

业务用途：菜品规格（大/中/小份等）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 规格ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `spec_name` | VARCHAR(50) | 否 | — |  | 规格名称 |
| `price_delta` | DECIMAL(10,2) | 否 | 0.00 |  | 加价金额(元) |
| `sort` | INT | 否 | 0 |  | 排序号 |
| `status` | TINYINT | 否 | 1 |  | 状态 1启用 0停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)

**逻辑外键：**
- `dish_spec.store_id` → `store.id`（CASCADE）

### ingredient_category — 原料类别表

业务用途：原料类别（生鲜/粮油/调料等）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 原料类别ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `category_name` | VARCHAR(50) | 否 | — |  | 类别名称 |
| `sort` | INT | 否 | 0 |  | 排序号 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)

**逻辑外键：**
- `ingredient_category.store_id` → `store.id`（CASCADE）

### ingredient — 原料表

业务用途：原料档案（名称/规格/单位/参考成本）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 原料ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `category_id` | BIGINT UNSIGNED | 是 | NULL |  | 原料类别ID |
| `ingredient_name` | VARCHAR(50) | 否 | — |  | 原料名称 |
| `unit` | VARCHAR(20) | 是 | NULL |  | 计量单位 |
| `spec` | VARCHAR(50) | 是 | NULL |  | 规格说明 |
| `status` | TINYINT | 否 | 1 |  | 状态 1启用 0停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)
- `INDEX` `idx_category_id` (`category_id`)

**逻辑外键：**
- `ingredient.store_id` → `store.id`（CASCADE）
- `ingredient.category_id` → `ingredient_category.id`（RESTRICT）

### dish — 菜品表

业务用途：菜品主数据（价格/分类/口味/规格/上下架/销量）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 菜品ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `category_id` | BIGINT UNSIGNED | 否 | — |  | 菜品分类ID |
| `dish_name` | VARCHAR(100) | 否 | — |  | 菜品名称 |
| `dish_no` | VARCHAR(20) | 是 | NULL |  | 菜品编号 |
| `image` | VARCHAR(255) | 是 | NULL |  | 菜品图片URL |
| `description` | VARCHAR(500) | 是 | NULL |  | 菜品描述 |
| `price` | DECIMAL(10,2) | 否 | — |  | 单价(元) |
| `origin_price` | DECIMAL(10,2) | 是 | NULL |  | 划线原价(元)，用于展示促销 |
| `unit` | VARCHAR(20) | 是 | '份' |  | 售卖单位(份/杯/例等) |
| `taste_id` | BIGINT UNSIGNED | 是 | NULL |  | 默认口味ID |
| `spec_id` | BIGINT UNSIGNED | 是 | NULL |  | 默认规格ID |
| `is_recommend` | TINYINT | 否 | 0 |  | 是否推荐 1是 0否 |
| `is_new` | TINYINT | 否 | 0 |  | 是否新品 1是 0否 |
| `is_sold_out` | TINYINT | 否 | 0 |  | 是否售罄 1是 0否 |
| `sale_count` | INT | 否 | 0 |  | 累计销量（销售排行用） |
| `sort` | INT | 否 | 0 |  | 排序号 |
| `status` | TINYINT | 否 | 1 |  | 状态 1上架 0下架 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_category` (`store_id`, `category_id`)
- `INDEX` `idx_status` (`status`)
- `INDEX` `idx_fk_category_id` (`category_id`)
- `INDEX` `idx_fk_taste_id` (`taste_id`)
- `INDEX` `idx_fk_spec_id` (`spec_id`)

**逻辑外键：**
- `dish.store_id` → `store.id`（CASCADE）
- `dish.category_id` → `dish_category.id`（RESTRICT）
- `dish.taste_id` → `dish_taste.id`（RESTRICT）
- `dish.spec_id` → `dish_spec.id`（RESTRICT）

### dish_ingredient_rel — 菜品-原料关联表

业务用途：菜品与原料多对多关联，支撑成本核算与反查

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 关联ID |
| `dish_id` | BIGINT UNSIGNED | 否 | — |  | 菜品ID |
| `ingredient_id` | BIGINT UNSIGNED | 否 | — |  | 原料ID |
| `quantity` | DECIMAL(10,2) | 否 | 0.00 |  | 用量 |
| `unit` | VARCHAR(20) | 是 | NULL |  | 单位 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_dish_ingredient` (`dish_id`, `ingredient_id`)（唯一）
- `INDEX` `idx_ingredient_id` (`ingredient_id`)

**逻辑外键：**
- `dish_ingredient_rel.dish_id` → `dish.id`（CASCADE）
- `dish_ingredient_rel.ingredient_id` → `ingredient.id`（CASCADE）

### dish_review — 菜品评价表

业务用途：菜品评价（评分/图文/匿名/商家回复）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 评价ID |
| `order_id` | BIGINT UNSIGNED | 是 | NULL |  | 订单ID |
| `order_detail_id` | BIGINT UNSIGNED | 是 | NULL |  | 订单明细ID |
| `dish_id` | BIGINT UNSIGNED | 否 | — |  | 菜品ID |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `store_id` | BIGINT UNSIGNED | 是 | NULL |  | 门店ID |
| `rating` | TINYINT | 否 | 5 |  | 评分 1-5 |
| `content` | VARCHAR(500) | 是 | NULL |  | 评价内容 |
| `images` | VARCHAR(500) | 是 | NULL |  | 评价图片(逗号分隔) |
| `is_anonymous` | TINYINT | 否 | 0 |  | 是否匿名 1是 0否 |
| `reply` | VARCHAR(500) | 是 | NULL |  | 商家回复 |
| `reply_time` | DATETIME | 是 | NULL |  | 回复时间 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_dish_id` (`dish_id`)
- `INDEX` `idx_member_id` (`member_id`)
- `INDEX` `idx_order_id` (`order_id`)
- `INDEX` `idx_store_rating` (`store_id`, `rating`)

**逻辑外键：**
- `dish_review.dish_id` → `dish.id`（CASCADE）
- `dish_review.member_id` → `member.id`（RESTRICT）


## 五、台桌、排队、预约

### table_type — 桌型表

业务用途：桌型定义（2人/4人/6人/包间）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 桌型ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `type_name` | VARCHAR(50) | 否 | — |  | 桌型名称 |
| `max_persons` | INT | 否 | 4 |  | 最大容纳人数 |
| `sort` | INT | 否 | 0 |  | 排序号 |
| `status` | TINYINT | 否 | 1 |  | 状态 1启用 0停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)

**逻辑外键：**
- `table_type.store_id` → `store.id`（CASCADE）

### dining_table — 台桌表

业务用途：台桌档案（桌号/桌型/二维码/状态）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 台桌ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `table_type_id` | BIGINT UNSIGNED | 否 | — |  | 桌型ID |
| `table_no` | VARCHAR(20) | 否 | — |  | 桌号 |
| `table_name` | VARCHAR(50) | 是 | NULL |  | 桌位名称 |
| `qr_code` | VARCHAR(255) | 是 | NULL |  | 桌位二维码(扫码点餐) |
| `status` | TINYINT | 否 | 1 |  | 台桌状态 1空闲 2待点餐 3用餐中 4预结账 5已结账 |
| `person_count` | INT | 否 | 0 |  | 当前用餐人数 |
| `enable_reservation` | TINYINT | 否 | 1 |  | 是否可预约 1是 0否 |
| `sort` | INT | 否 | 0 |  | 排序号 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_store_table_no` (`store_id`, `table_no`)（唯一）
- `INDEX` `idx_table_type` (`table_type_id`)

**逻辑外键：**
- `dining_table.store_id` → `store.id`（CASCADE）
- `dining_table.table_type_id` → `table_type.id`（RESTRICT）

### queue — 排队取号记录表

业务用途：排队取号记录（叫号/过号/就位），支撑取号叫号流程

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 排队ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `queue_no` | VARCHAR(20) | 否 | — |  | 排队号(如A001) |
| `table_type_id` | BIGINT UNSIGNED | 是 | NULL |  | 期望桌型ID |
| `persons` | INT | 否 | 1 |  | 就餐人数 |
| `member_id` | BIGINT UNSIGNED | 是 | NULL |  | 客户ID |
| `phone` | VARCHAR(20) | 是 | NULL |  | 联系电话 |
| `status` | TINYINT | 否 | 1 |  | 状态 1排队中 2已叫号 3已过号 4已就位 5已取消 |
| `call_count` | INT | 否 | 0 |  | 叫号次数 |
| `over_count` | INT | 否 | 0 |  | 过号次数 |
| `call_time` | DATETIME | 是 | NULL |  | 最近叫号时间 |
| `seated_time` | DATETIME | 是 | NULL |  | 就位时间 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 取号时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_status` (`store_id`, `status`)
- `INDEX` `idx_store_time` (`store_id`, `create_time`)
- `INDEX` `idx_table_type` (`table_type_id`)

**逻辑外键：**
- `queue.store_id` → `store.id`（CASCADE）
- `queue.table_type_id` → `table_type.id`（RESTRICT）

### reservation — 餐桌预约表

业务用途：餐桌预约（时段/人数/预留桌）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 预约ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `table_id` | BIGINT UNSIGNED | 是 | NULL |  | 台桌ID |
| `table_type_id` | BIGINT UNSIGNED | 是 | NULL |  | 桌型ID |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `contact_name` | VARCHAR(50) | 否 | — |  | 联系人姓名 |
| `contact_phone` | VARCHAR(20) | 否 | — |  | 联系电话 |
| `reservation_date` | DATE | 否 | — |  | 预约日期 |
| `reservation_time` | TIME | 否 | — |  | 预约时间 |
| `persons` | INT | 否 | 1 |  | 就餐人数 |
| `remark` | VARCHAR(255) | 是 | NULL |  | 备注 |
| `status` | TINYINT | 否 | 1 |  | 状态 1待确认 2预约成功 3已取消 4已完成 |
| `confirm_time` | DATETIME | 是 | NULL |  | 确认时间 |
| `complete_time` | DATETIME | 是 | NULL |  | 完成时间 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_date` (`store_id`, `reservation_date`)
- `INDEX` `idx_member_id` (`member_id`)
- `INDEX` `idx_table_id` (`table_id`)

**逻辑外键：**
- `reservation.store_id` → `store.id`（CASCADE）
- `reservation.member_id` → `member.id`（RESTRICT）
- `reservation.table_id` → `dining_table.id`（RESTRICT）


## 六、订单

### cart — 购物车表

业务用途：购物车（会员会话级点餐暂存）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 购物车ID |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `dish_id` | BIGINT UNSIGNED | 否 | — |  | 菜品ID |
| `dish_name` | VARCHAR(100) | 是 | NULL |  | 菜品名称快照 |
| `dish_image` | VARCHAR(255) | 是 | NULL |  | 菜品图片快照 |
| `spec_name` | VARCHAR(50) | 是 | NULL |  | 规格快照 |
| `taste_name` | VARCHAR(50) | 是 | NULL |  | 口味快照 |
| `price` | DECIMAL(10,2) | 否 | — |  | 加入时单价 |
| `quantity` | INT | 否 | 1 |  | 数量 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 加入时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_cart_dish` (`member_id`, `store_id`, `dish_id`, `spec_name`, `taste_name`)（唯一）
- `INDEX` `idx_dish_id` (`dish_id`)

**逻辑外键：**
- `cart.member_id` → `member.id`（CASCADE）
- `cart.dish_id` → `dish.id`（RESTRICT）

### orders — 订单主表（coupon_id为逻辑外键，关联coupon.id）

业务用途：订单主表（堂食/外卖，金额拆分，状态机，乐观锁）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 订单ID |
| `order_no` | VARCHAR(32) | 否 | — |  | 订单号 |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `order_type` | TINYINT | 否 | — |  | 订单类型 1堂食 2外卖 3自取 |
| `table_id` | BIGINT UNSIGNED | 是 | NULL |  | 台桌ID(堂食) |
| `table_type_id` | BIGINT UNSIGNED | 是 | NULL |  | 桌型ID(堂食) |
| `queue_id` | BIGINT UNSIGNED | 是 | NULL |  | 排队ID |
| `person_count` | INT | 否 | 1 |  | 用餐人数 |
| `order_status` | TINYINT | 否 | 1 |  | 订单状态 1待支付 2待制作 3制作中 4待配送 5配送中 6待自取 7已完成 8已取消 9已退款 |
| `pay_status` | TINYINT | 否 | 1 |  | 支付状态 1待支付 2已支付 3已退款 4部分退款 |
| `take_type` | TINYINT | 是 | NULL |  | 外卖取餐方式 1外送 2自取 |
| `address_id` | BIGINT UNSIGNED | 是 | NULL |  | 收货地址ID |
| `address_snapshot` | VARCHAR(1000) | 是 | NULL |  | 地址快照(下单时保存) |
| `dish_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 菜品原价合计 |
| `discount_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 优惠/折扣金额 |
| `free_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 免单金额 |
| `rounding_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 抹零金额 |
| `coupon_id` | BIGINT UNSIGNED | 是 | NULL |  | 使用的优惠券ID |
| `coupon_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 优惠券抵扣金额 |
| `delivery_fee` | DECIMAL(10,2) | 否 | 0.00 |  | 配送费 |
| `payable_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 应付金额 |
| `actual_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 实付金额 |
| `member_pay_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 会员余额支付金额 |
| `source` | TINYINT | 否 | 1 |  | 订单来源 1客户端 2服务员 3收银 |
| `operator_id` | BIGINT UNSIGNED | 是 | NULL |  | 操作员工ID |
| `remark` | VARCHAR(255) | 是 | NULL |  | 订单备注 |
| `order_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 下单时间 |
| `pay_time` | DATETIME | 是 | NULL |  | 支付时间 |
| `expected_time` | DATETIME | 是 | NULL |  | 预计送达/自取时间 |
| `shipping_time` | DATETIME | 是 | NULL |  | 配送/出餐时间 |
| `version` | INT | 否 | 0 |  | 乐观锁版本号（防止状态并发覆盖） |
| `finish_time` | DATETIME | 是 | NULL |  | 完成时间 |
| `cancel_time` | DATETIME | 是 | NULL |  | 取消时间 |
| `cancel_reason` | VARCHAR(255) | 是 | NULL |  | 取消原因 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_order_no` (`order_no`)（唯一）
- `INDEX` `idx_store_status` (`store_id`, `order_status`)
- `INDEX` `idx_member_id` (`member_id`)
- `INDEX` `idx_member_time` (`member_id`, `order_time`)
- `INDEX` `idx_order_time` (`order_time`)
- `INDEX` `idx_table_id` (`table_id`)
- `INDEX` `idx_coupon_id` (`coupon_id`)

**逻辑外键：**
- `orders.store_id` → `store.id`（RESTRICT）
- `orders.member_id` → `member.id`（RESTRICT）
- `orders.table_id` → `dining_table.id`（RESTRICT）
- `orders.coupon_id` → `coupon.id`（LOGICAL）

### order_detail — 订单明细表

业务用途：订单明细（菜品快照/数量/小计/制作状态/已退金额）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 明细ID |
| `order_id` | BIGINT UNSIGNED | 否 | — |  | 订单ID |
| `dish_id` | BIGINT UNSIGNED | 否 | — |  | 菜品ID |
| `dish_name` | VARCHAR(100) | 否 | — |  | 菜品名称快照 |
| `dish_image` | VARCHAR(255) | 是 | NULL |  | 菜品图片快照 |
| `dish_price` | DECIMAL(10,2) | 否 | — |  | 成交单价(元) |
| `spec_name` | VARCHAR(50) | 是 | NULL |  | 规格快照 |
| `taste_name` | VARCHAR(50) | 是 | NULL |  | 口味快照 |
| `quantity` | INT | 否 | 1 |  | 数量 |
| `subtotal` | DECIMAL(10,2) | 否 | 0.00 |  | 小计金额 |
| `refund_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 已退金额(元)，退菜时累加 |
| `status` | TINYINT | 否 | 1 |  | 制作状态 1待制作 2制作中 3已上齐 4退菜 |
| `remark` | VARCHAR(255) | 是 | NULL |  | 备注 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_order_id` (`order_id`)
- `INDEX` `idx_dish_id` (`dish_id`)
- `INDEX` `idx_status` (`status`)

**逻辑外键：**
- `order_detail.order_id` → `orders.id`（CASCADE）
- `order_detail.dish_id` → `dish.id`（RESTRICT）

### order_status_log — 订单状态日志表

业务用途：订单状态流转日志，业务审计留痕

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 日志ID |
| `order_id` | BIGINT UNSIGNED | 否 | — |  | 订单ID |
| `from_status` | TINYINT | 是 | NULL |  | 原状态 |
| `to_status` | TINYINT | 否 | — |  | 新状态 |
| `operator_type` | VARCHAR(20) | 否 | 'SYSTEM' |  | 操作方 CLIENT客户端 STAFF员工 SYSTEM系统 |
| `operator_id` | BIGINT UNSIGNED | 是 | NULL |  | 操作者ID |
| `remark` | VARCHAR(255) | 是 | NULL |  | 说明 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_order_id` (`order_id`)

**逻辑外键：**
- `order_status_log.order_id` → `orders.id`（CASCADE）

### payment_record — 支付记录表

业务用途：支付记录（渠道/幂等键/交易号/回调）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 支付记录ID |
| `pay_no` | VARCHAR(32) | 否 | — |  | 支付流水号 |
| `order_id` | BIGINT UNSIGNED | 否 | — |  | 订单ID |
| `store_id` | BIGINT UNSIGNED | 是 | NULL |  | 门店ID |
| `member_id` | BIGINT UNSIGNED | 是 | NULL |  | 客户ID |
| `pay_type` | VARCHAR(20) | 否 | — |  | 支付类型 WECHAT微信 ALIPAY支付宝 MEMBER_BALANCE会员余额 CASH现金 SCAN扫码付 |
| `pay_channel` | VARCHAR(50) | 是 | NULL |  | 支付渠道说明 |
| `amount` | DECIMAL(10,2) | 否 | — |  | 支付金额(元) |
| `status` | TINYINT | 否 | 1 |  | 状态 1待支付 2成功 3失败 4已退款 |
| `transaction_id` | VARCHAR(64) | 是 | NULL |  | 第三方交易号 |
| `idempotency_key` | VARCHAR(64) | 是 | NULL |  | 幂等键（防重复支付回调，同一订单同渠道唯一） |
| `payer_account` | VARCHAR(100) | 是 | NULL |  | 付款账号（脱敏展示） |
| `notify_time` | DATETIME | 是 | NULL |  | 支付回调通知时间 |
| `pay_time` | DATETIME | 是 | NULL |  | 支付时间 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_pay_no` (`pay_no`)（唯一）
- `UNIQUE` `uk_idempotency_key` (`idempotency_key`)（唯一）
- `INDEX` `idx_order_id` (`order_id`)
- `INDEX` `idx_store_id` (`store_id`)
- `INDEX` `idx_member_id` (`member_id`)
- `INDEX` `idx_pay_time` (`pay_time`)

**逻辑外键：**
- `payment_record.order_id` → `orders.id`（RESTRICT）
- `payment_record.store_id` → `store.id`（RESTRICT）
- `payment_record.member_id` → `member.id`（RESTRICT）

### refund — 退单记录表

业务用途：退单/退款记录（退菜/整单，金额与原因）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 退单ID |
| `refund_no` | VARCHAR(32) | 否 | — |  | 退单号 |
| `order_id` | BIGINT UNSIGNED | 否 | — |  | 订单ID |
| `order_detail_id` | BIGINT UNSIGNED | 是 | NULL |  | 订单明细ID(退菜时) |
| `store_id` | BIGINT UNSIGNED | 是 | NULL |  | 门店ID |
| `member_id` | BIGINT UNSIGNED | 是 | NULL |  | 客户ID |
| `refund_type` | TINYINT | 否 | 1 |  | 退单类型 1整单退 2退菜 |
| `dish_name` | VARCHAR(100) | 是 | NULL |  | 退菜名称 |
| `quantity` | INT | 是 | NULL |  | 退菜数量 |
| `amount` | DECIMAL(10,2) | 否 | — |  | 退款金额(元) |
| `reason` | VARCHAR(255) | 是 | NULL |  | 退单原因 |
| `status` | TINYINT | 否 | 1 |  | 状态 1待审核 2已通过 3已驳回 4已完成 |
| `operator_id` | BIGINT UNSIGNED | 是 | NULL |  | 操作员工ID |
| `refund_time` | DATETIME | 是 | NULL |  | 退款时间 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_refund_no` (`refund_no`)（唯一）
- `INDEX` `idx_order_id` (`order_id`)
- `INDEX` `idx_store_id` (`store_id`)
- `INDEX` `idx_store_time` (`store_id`, `create_time`)

**逻辑外键：**
- `refund.order_id` → `orders.id`（RESTRICT）


## 七、促销（优惠券）

### coupon — 优惠券表

业务用途：优惠券定义（满减/折扣/立减，使用范围）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 优惠券ID |
| `store_id` | BIGINT UNSIGNED | 是 | NULL |  | 门店ID(NULL为通用券) |
| `coupon_name` | VARCHAR(50) | 否 | — |  | 优惠券名称 |
| `coupon_type` | TINYINT | 否 | 1 |  | 类型 1满减 2折扣 3立减 |
| `threshold_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 使用门槛金额(元) |
| `discount_amount` | DECIMAL(10,2) | 否 | 0.00 |  | 减免金额(元)(满减/立减) |
| `discount_rate` | DECIMAL(3,2) | 是 | NULL |  | 折扣率(折扣券) |
| `total_count` | INT | 否 | 0 |  | 发行总量(0不限量) |
| `issued_count` | INT | 否 | 0 |  | 已发放数量 |
| `per_user_limit` | INT | 否 | 1 |  | 每人限领数量 |
| `valid_type` | TINYINT | 否 | 1 |  | 有效期类型 1固定时间 2领取后N天 |
| `valid_start` | DATETIME | 是 | NULL |  | 生效开始时间 |
| `valid_end` | DATETIME | 是 | NULL |  | 生效结束时间 |
| `valid_days` | INT | 是 | NULL |  | 领取后有效天数 |
| `use_scope` | TINYINT | 否 | 1 |  | 使用范围 1全场 2指定分类 |
| `scope_category_id` | BIGINT UNSIGNED | 是 | NULL |  | 适用菜品分类ID（use_scope=2时） |
| `status` | TINYINT | 否 | 1 |  | 状态 1草稿 2已发布 3已结束 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |
| `is_deleted` | TINYINT | 否 | 0 |  | 逻辑删除 0否 1是 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)
- `INDEX` `idx_status` (`status`)

**逻辑外键：**
- `coupon.store_id` → `store.id`（RESTRICT）

### member_coupon — 用户优惠券表

业务用途：会员领用优惠券（状态/有效期/使用订单）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 用户券ID |
| `member_id` | BIGINT UNSIGNED | 否 | — |  | 客户ID |
| `coupon_id` | BIGINT UNSIGNED | 否 | — |  | 优惠券ID |
| `store_id` | BIGINT UNSIGNED | 是 | NULL |  | 适用门店ID |
| `status` | TINYINT | 否 | 1 |  | 状态 1未使用 2已使用 3已过期 |
| `receive_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 领取时间 |
| `expire_time` | DATETIME | 是 | NULL |  | 过期时间 |
| `use_time` | DATETIME | 是 | NULL |  | 使用时间 |
| `order_id` | BIGINT UNSIGNED | 是 | NULL |  | 使用订单ID |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_member_id` (`member_id`)
- `INDEX` `idx_coupon_id` (`coupon_id`)
- `INDEX` `idx_status` (`status`)
- `INDEX` `idx_expire` (`status`, `expire_time`)

**逻辑外键：**
- `member_coupon.member_id` → `member.id`（CASCADE）
- `member_coupon.coupon_id` → `coupon.id`（CASCADE）


## 八、库存

### dish_stock — 菜品库存表

业务用途：菜品库存（按门店按菜品）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 菜品库存ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `dish_id` | BIGINT UNSIGNED | 否 | — |  | 菜品ID |
| `stock_quantity` | INT | 否 | 0 |  | 当前库存数量 |
| `warn_threshold` | INT | 否 | 0 |  | 预警阈值 |
| `unit` | VARCHAR(20) | 是 | NULL |  | 计量单位(份) |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_store_dish` (`store_id`, `dish_id`)（唯一）
- `INDEX` `idx_fk_dish_id` (`dish_id`)

**逻辑外键：**
- `dish_stock.store_id` → `store.id`（CASCADE）
- `dish_stock.dish_id` → `dish.id`（CASCADE）

### ingredient_stock — 原料库存表

业务用途：原料库存（按门店按原料）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 原料库存ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `ingredient_id` | BIGINT UNSIGNED | 否 | — |  | 原料ID |
| `stock_quantity` | DECIMAL(10,2) | 否 | 0.00 |  | 当前库存数量 |
| `warn_threshold` | DECIMAL(10,2) | 否 | 0.00 |  | 预警阈值 |
| `unit` | VARCHAR(20) | 是 | NULL |  | 计量单位 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_store_ingredient` (`store_id`, `ingredient_id`)（唯一）
- `INDEX` `idx_fk_ingredient_id` (`ingredient_id`)

**逻辑外键：**
- `ingredient_stock.store_id` → `store.id`（CASCADE）
- `ingredient_stock.ingredient_id` → `ingredient.id`（CASCADE）

### stock_check_dish — 菜品盘点单表

业务用途：菜品盘点单（盘点批次/差异）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 盘点单ID |
| `check_no` | VARCHAR(32) | 否 | — |  | 盘点单号 |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `dish_id` | BIGINT UNSIGNED | 否 | — |  | 菜品ID |
| `stock_before` | INT | 否 | 0 |  | 盘点前库存 |
| `stock_after` | INT | 否 | 0 |  | 盘点后库存 |
| `diff` | INT | 否 | 0 |  | 差异(盘后-盘前) |
| `reason` | VARCHAR(255) | 是 | NULL |  | 差异原因 |
| `status` | TINYINT | 否 | 1 |  | 状态 1待审核 2已通过 3已驳回 |
| `creator_id` | BIGINT UNSIGNED | 是 | NULL |  | 创建人(店长) |
| `auditor_id` | BIGINT UNSIGNED | 是 | NULL |  | 审核人(总店长) |
| `audit_time` | DATETIME | 是 | NULL |  | 审核时间 |
| `audit_remark` | VARCHAR(255) | 是 | NULL |  | 审核意见 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_check_no` (`check_no`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)
- `INDEX` `idx_status` (`status`)
- `INDEX` `idx_fk_dish_id` (`dish_id`)

**逻辑外键：**
- `stock_check_dish.store_id` → `store.id`（RESTRICT）
- `stock_check_dish.dish_id` → `dish.id`（RESTRICT）

### stock_check_ingredient — 原料盘点单表

业务用途：原料盘点单（盘点批次/差异）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 盘点单ID |
| `check_no` | VARCHAR(32) | 否 | — |  | 盘点单号 |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `ingredient_id` | BIGINT UNSIGNED | 否 | — |  | 原料ID |
| `stock_before` | DECIMAL(10,2) | 否 | 0.00 |  | 盘点前库存 |
| `stock_after` | DECIMAL(10,2) | 否 | 0.00 |  | 盘点后库存 |
| `diff` | DECIMAL(10,2) | 否 | 0.00 |  | 差异(盘后-盘前) |
| `reason` | VARCHAR(255) | 是 | NULL |  | 差异原因 |
| `status` | TINYINT | 否 | 1 |  | 状态 1待审核 2已通过 3已驳回 |
| `creator_id` | BIGINT UNSIGNED | 是 | NULL |  | 创建人(店长) |
| `auditor_id` | BIGINT UNSIGNED | 是 | NULL |  | 审核人(总店长) |
| `audit_time` | DATETIME | 是 | NULL |  | 审核时间 |
| `audit_remark` | VARCHAR(255) | 是 | NULL |  | 审核意见 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_check_no` (`check_no`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)
- `INDEX` `idx_status` (`status`)
- `INDEX` `idx_fk_ingredient_id` (`ingredient_id`)

**逻辑外键：**
- `stock_check_ingredient.store_id` → `store.id`（RESTRICT）
- `stock_check_ingredient.ingredient_id` → `ingredient.id`（RESTRICT）


## 九、硬件与意见反馈

### printer — 打印机表

业务用途：门店打印机绑定（打印任务/启用状态）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 打印机ID |
| `store_id` | BIGINT UNSIGNED | 否 | — |  | 门店ID |
| `printer_name` | VARCHAR(50) | 否 | — |  | 打印机名称 |
| `printer_no` | VARCHAR(50) | 是 | NULL |  | 打印机编号 |
| `printer_type` | VARCHAR(20) | 是 | NULL |  | 用途 KITCHEN后厨 RECEIPT前台 TAKEOUT外卖 |
| `status` | TINYINT | 否 | 1 |  | 状态 1启用 0停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_store_id` (`store_id`)

**逻辑外键：**
- `printer.store_id` → `store.id`（CASCADE）

### receipt_template — 小票模板表

业务用途：小票模板（结账单/外卖单/排队单模板内容）

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 模板ID |
| `template_type` | VARCHAR(20) | 否 | — |  | 类型 DINE_IN堂食 TAKEOUT外卖 KITCHEN后厨 RECHARGE充值 |
| `template_name` | VARCHAR(50) | 否 | — |  | 模板名称 |
| `template_content` | TEXT | 是 | NULL |  | 模板内容 |
| `is_default` | TINYINT | 否 | 0 |  | 是否默认 1是 0否 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `UNIQUE` `uk_template_type` (`template_type`)（唯一）

### feedback — 意见反馈表

业务用途：客户意见反馈

| 字段名 | 数据类型 | 允许空 | 默认值 | 键 | 中文含义 |
|---|---|---|---|---|---|
| `id` | BIGINT UNSIGNED | 否 | — | AI | 反馈ID |
| `member_id` | BIGINT UNSIGNED | 是 | NULL |  | 客户ID |
| `store_id` | BIGINT UNSIGNED | 是 | NULL |  | 门店ID |
| `content` | VARCHAR(1000) | 否 | — |  | 反馈内容 |
| `images` | VARCHAR(500) | 是 | NULL |  | 反馈图片(逗号分隔) |
| `contact_phone` | VARCHAR(20) | 是 | NULL |  | 联系电话 |
| `status` | TINYINT | 否 | 1 |  | 状态 1待处理 2已处理 |
| `reply` | VARCHAR(500) | 是 | NULL |  | 处理回复 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP |  | 更新时间 |

**索引：**
- `PRIMARY` `PRIMARY` (`id`)（唯一）
- `INDEX` `idx_member_id` (`member_id`)
- `INDEX` `idx_status` (`status`)


## 附录 报表视图

| 视图名 | 名称 | 用途 |
|---|---|---|
| `v_dish_flow` | 菜品流水 | 订单明细级流水记录，支撑商户端【报表-菜品流水】 |
| `v_receipt_report` | 收款日报 | 按门店按天汇总实收，支撑【报表-收款报表】 |
| `v_dish_sales_ranking` | 菜品销售排行 | 按菜品汇总销量与销售额，支撑【报表-菜品销售排行】 |
| `v_store_sales_ranking` | 门店销售排行 | 按门店汇总单量与销售额，支撑【报表-门店销售排行】 |
| `v_today_income` | 今日收入 | 收银端/首页今日订单数与实收汇总 |
| `v_refund_record` | 退单记录 | 退单明细视图，支撑【报表-退单记录】 |
